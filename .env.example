# Copy this to .env.local for local development (git-ignored).
#
# For local dev, start the worker with:  npx wrangler dev
# It will listen on http://localhost:8787 by default.
#
# The Anthropic key lives ONLY in the Cloudflare Worker secret — never here.
VITE_PROXY_URL=http://localhost:8787
